// Theme configurations for MUI - extended from context
// Individual overrides if needed

export const themeOverrides = {
  dark: {
    components: {
      MuiCssBaseline: {
        styleOverrides: {
          body: {
            background: 'linear-gradient(135deg, #080c14 0%, #0d111a 100%)'
          }
        }
      }
    }
  },
  // Add more theme-specific overrides here
};
